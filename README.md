# thread-catch workspace

The publishable package lives in [`packages/thread-catch`](packages/bug-report-widget/README.md). A complete Next.js demonstration is in [`examples/next-app`](examples/next-app).
